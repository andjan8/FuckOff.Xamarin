<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FuckOff.MainPage">
 

  <ContentPage.ToolbarItems>
    <ToolbarItem Text="About"
               Order="Secondary"
                Clicked="OnAboutToolbarItemClicked" />
    <ToolbarItem Text="Settings"
              Order="Secondary"
               Clicked="OnSettingsToolbarItemClicked" />
  </ContentPage.ToolbarItems>
  <ContentPage.Content>

    <Grid x:Name="mainPageGrid" RowSpacing="5" ColumnSpacing="5">
      <Grid.RowDefinitions>
        <RowDefinition Height="*" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />

      </Grid.RowDefinitions>

      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <Label
        Text="{Binding FuckOffText}"
        Grid.Row="0"
        Grid.Column="0"
        Grid.ColumnSpan="3"
        Grid.RowSpan="2"/>
        
      <Button
        Text="Fuck Off!"
          Grid.Row="2"
             Grid.Column="1"
            Command="{Binding FuckOffCommand}" />

    </Grid>
  </ContentPage.Content>
</ContentPage>